import react from "react";
import LazyInputCard from "./LazyInputCard";
import "./LazyInput.css";

const LazyInput = () => {
  return (
    <div class="grid-container">
      <LazyInputCard />
    </div>
  );
};

export default LazyInput;
